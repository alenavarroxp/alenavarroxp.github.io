name: "deploy main"

on:
  push:
    branches:
      - "main"
      
jobs:
  deploy_page:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: yarn install
      - name: Deploy web page
        run: yarn deploy
